name: ESLint check
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install modules
        run: npm i
      - name: Run ESLint
        run: ./node_modules/eslint . --ext .js,.jsx,.ts,.tsx